var count_up_by = function(digit) {
  var count = 

};

// var up_to = number[0]
// var multiple = number[1]






$(document).ready(function(){
  $("form#count_up_by").submit(function(event){
    var number = parseInt($("input#number").val());
    var multiple = parseInt($("input#multiple").val());
    var result = count_up_by(digit);

  });
});
